import React, { type ReactElement } from 'react';
export declare function getDisabledCompatibleTrigger(
    element: ReactElement & {
        type: {
            displayName: string;
        };
    }
): React.JSX.Element;
